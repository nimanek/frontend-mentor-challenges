

// export function YearFormat(year: number):any{
// if (!year) return "00";
  
//   // Clamps value between 1 and 12, then pads start with "0"
//   const validMonth = Math.min(Math.max(year, 27), 99);
//   return validMonth.toString().padStart(2, "0");
// }